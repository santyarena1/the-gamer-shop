# Copiá a .env.local (git lo ignora; tus datos no se pisan en pull/push):
#   copy .env.local.example .env.local
#
# Contraseña: la de pgAdmin, o ejecutá scripts/reset-postgres-password.ps1 como Admin → devlocal

DATABASE_URL="postgresql://postgres@127.0.0.1:5432/thegamershop"
POSTGRES_PASSWORD=devlocal

NEXTAUTH_URL="http://localhost:3000"
